# Rust Image Viewer

Rust + Tauri + Vue 3 による軽量デスクトップ画像ビューアです。フォルダ・ZIP・RAR アーカイブから画像を読み込み、高速に表示します。
MangaMeeyaの代替として作成し、使用しています。

## 動作環境

- Windows 10 / 11 (x64)
- Rust 1.94+
- Node.js 22+
- npm 11+

## ビルド

```bash
npm install
npm run tauri build
```

生成物:

| 成果物 | パス |
|--------|------|
| 実行ファイル | `src-tauri/target/release/rust-image-viewer.exe` |
| MSI インストーラ | `src-tauri/target/release/bundle/msi/Rust Image Viewer_1.0.0_x64_en-US.msi` |
| NSIS インストーラ | `src-tauri/target/release/bundle/nsis/Rust Image Viewer_1.0.0_x64-setup.exe` |

開発時は `npm run tauri dev` でホットリロード開発が可能です。

## 対応画像形式

| 形式 | 拡張子 | 備考 |
|------|--------|------|
| JPEG | `.jpg` `.jpeg` | Rust 側でリサイズ |
| PNG | `.png` | Rust 側でリサイズ |
| BMP | `.bmp` | Rust 側でリサイズ |
| WebP | `.webp` | ブラウザネイティブ表示 |
| AVIF | `.avif` | ブラウザネイティブ表示 |
| GIF | `.gif` | ブラウザネイティブ表示 |

## 対応ソース

- **フォルダ**: 指定したディレクトリ以下を再帰的に走査
- **ZIP アーカイブ**: `.zip` ファイル内の画像を直接読み込み
- **RAR アーカイブ**: `.rar` ファイル内の画像を直接読み込み

いずれもファイル名を自然順（numerical sort）で並び替えます（例: `1.jpg, 2.jpg, 10.jpg, 100.jpg`）。

## 操作方法

### ファイルの読み込み

| 操作 | 説明 |
|------|------|
| ドラッグ＆ドロップ | アプリ内にフォルダ/ZIP/RAR をドロップ |
| クリックでフォルダ選択 | ドロップゾーンをクリックしてフォルダ選択ダイアログを開く |
| アーカイブ選択 | ドロップゾーンの「アーカイブ選択」リンクから ZIP/RAR ファイルを選択 |
| exe に D&D | `rust-image-viewer.exe` にファイルをドロップして起動 |
| コマンドライン | `rust-image-viewer.exe "path/to/file.zip"` |

### ページ操作

| 操作 | デフォルトキー | 備考 |
|------|----------------|------|
| 次のページ | `←` | 最後のページで折り返し |
| 前のページ | `→` | 最初のページで折り返し |
| 最初のページ | `Home` | |
| 最後のページ | `End` | |
| 左クリック | 次のページ | |
| 右クリック | 前のページ | コンテキストメニューは無効 |
| マウスホイール | ページ送り | 上＝前、下＝次 |

### 表示切替

| 操作 | デフォルトキー | 備考 |
|------|----------------|------|
| 全画面 / ウィンドウ切替 | `Enter` | 全画面時はスライダー・ステータスバーを非表示 |
| 単ページ / 見開き切替 | `Space` | 見開きは 2 ページ同時表示 |
| 設定パネル | `Esc` | トグル動作（開く/閉じる） |

### ページスライダー

画面下部に全ページ数のスライダーを配置。ドラッグで任意のページにジャンプできます。
スライダー左側に `⏮`（最初へ）、`⏭`（最後へ）のボタンがあります。

## 設定

`Esc` キーで設定パネルを開き、以下を変更できます。設定は `%APPDATA%\rust-image-viewer\settings.json` に保存されます。

### キーバインド

7 種類のアクションに対してキーを割り当てられます。項目をクリックしてキーを押すことで変更できます。修飾キー（Ctrl / Alt / Shift）との組み合わせも対応しています。

### 表示

| 項目 | 説明 |
|------|------|
| 背景色 | カラーピッカーで指定 |

### 動作

| 項目 | デフォルト値 | 説明 |
|------|-------------|------|
| 先読み枚数 | 10 | 現在のページの前後に事前読み込みする枚数 |
| キー反応間隔 | 80 ms | キーボード入力のスロットル間隔 |
| ホイール反応間隔 | 150 ms | マウスホイールのスロットル間隔 |

### 起動時の状態復元

終了時に以下の状態が自動的に保存され、次回起動時に復元されます。

- ウィンドウサイズ
- 表示モード（単ページ / 見開き）

## アーキテクチャ

```
┌─────────────────────────────────────────────────┐
│                  Vue 3 Frontend                 │
│  App.vue                                        │
│  ├── DropZone.vue        起動画面 / D&D         │
│  ├── ImageViewer.vue     画像表示               │
│  ├── PageSlider.vue      ページスライダー       │
│  ├── StatusBar.vue       ページ情報             │
│  ├── SettingsPanel.vue   設定モーダル           │
│  └── Notification.vue    トースト通知           │
│                                                 │
│  Composables (Module-level Singleton):           │
│  ├── useImageStore       画像キャッシュ・先読み  │
│  ├── usePageController   ページ制御             │
│  ├── useKeyBindings      キーボードハンドリング  │
│  ├── useSettings         設定の読込・保存        │
│  └── useFullscreen       フルスクリーン状態管理  │
│                                                 │
│  Utils:                                         │
│  └── keyCombo             キー組み合わせの構築・表示 │
└────────────────────┬────────────────────────────┘
                     │ Tauri IPC
┌────────────────────▼────────────────────────────┐
│                 Rust Backend                    │
│  commands/                                      │
│  ├── file_loader         フォルダ/ZIP/RAR 列挙  │
│  ├── image_provider      画像読込・リサイズ      │
│  └── settings            設定の永続化            │
│  archive/                                       │
│  ├── zip_handler         ZIP 読み込み           │
│  └── rar_handler         RAR 読み込み           │
│  models/                                        │
│  ├── image_entry         データモデル            │
│  └── settings            設定スキーマ            │
│  utils/                                         │
│  └── sorter               自然順ソート           │
└─────────────────────────────────────────────────┘
```

## 主な設計方針

- **サーバーサイドリサイズ**: 画像は Rust 側でビューポートに合わせてリサイズ（Lanczos3）してから転送し、メモリ・帯域を節約
- **AVIF/WebP/GIF のパススルー**: これらの形式は Rust 側でデコードせず raw データをブラウザに直接渡し、WebView2 のネイティブデコーダを活用
- **先読みキャッシュ**: 現在のページの前後を非同期で先読みし、スムーズなページ送りを実現（先読み枚数は設定可能）
- **世代管理**: 非同期画像取得に世代カウンターを用い、古いリクエストの結果は破棄して表示崩れを防止
- **入力スロットリング**: キーボード・マウスホイールのスロットリング間隔を設定可能にし、操作詰まりを防止
- **全画面切替の即時追従**: キャッシュを無効化しつつ古い画像は表示し続け、新しい解像度の画像取得後にシームレスに差し替え
- **見開き表示の最適化**: 左画像を右寄せ・右画像を左寄せに配置し、綴じ目での隙間を解消
- **設定の後方互換性**: `#[serde(default)]` により、新しいバージョンでフィールドが追加されても古い設定ファイルを壊さない
- **CSP 有効化**: Content Security Policy により XSS 攻撃を防止
